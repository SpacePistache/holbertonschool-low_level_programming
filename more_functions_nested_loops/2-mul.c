#include "main.h"
/**
 * mul - multiplies two integers.
 *@a: is the first integer.
 *@b: is second integer.
 * Return: the result of this calculation.
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
